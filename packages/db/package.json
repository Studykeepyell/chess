{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:dev": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "test": "ts-node tests/db.test.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "5.12.1"
  },
  "devDependencies": {
    "prisma": "5.12.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
